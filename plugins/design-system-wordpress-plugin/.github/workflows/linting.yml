name: Trigger Linting

on:
  pull_request:
    branches:
      - development
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trigger:
    uses: bcgov/des-git-workflows/.github/workflows/linting.yml@main

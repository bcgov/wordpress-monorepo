name: Trigger Linting

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  trigger:
    permissions:
      contents: read
    uses: bcgov/des-git-workflows/.github/workflows/linting.yml@1.1.0
